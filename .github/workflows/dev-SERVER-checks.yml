name: dev SERVER checks

on:
  schedule:
    - cron: '0 13 * * 3'  # Wednesday 16:00 Moscow time (UTC+3)
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check-server:
    runs-on: ubuntu-24.04
    name: dev SERVER checks x64

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Puppeteer
        uses: ./.github/actions/setup-puppeteer
        with:
          git_ssh_private_key: ${{ secrets.GIT_SSH_PRIVATE_KEY }}

      - name: Configure AWS CLI
        run: |
          aws configure set aws_access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws configure set aws_secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws configure set default.region us-east-1

      - name: Start EC2 Instance
        run: |
          aws ec2 start-instances --instance-ids ${{ secrets.AWS_DEV_SERVER_INSTANCE_ID }}
          echo "Waiting for instance to start..."
          aws ec2 wait instance-running --instance-ids ${{ secrets.AWS_DEV_SERVER_INSTANCE_ID }}

      - name: Add SSH Key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.AWS_SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          eval $(ssh-agent -s)
          ssh-add ~/.ssh/id_rsa

      - name: Wait for SSH to be Ready
        run: |
          for i in $(seq 1 10); do
            if ssh -o StrictHostKeyChecking=no -o ConnectTimeout=5 ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} exit 2>/dev/null; then
              echo "SSH is ready"
              exit 0
            fi
            echo "Attempt $i/10 — Waiting for SSH..."
            sleep 10
          done
          echo "SSH not available after 10 attempts"
          exit 1

      - name: Update EC2 Instance
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo apt-get -o DPkg::Lock::Timeout=600 update && sudo apt-get -o DPkg::Lock::Timeout=600 upgrade -y"

      - name: Reboot EC2 Instance
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} "sudo reboot" || true

      - name: Wait for instance to reboot
        run: |
          sleep 60
          for i in $(seq 1 10); do
            if ssh -o StrictHostKeyChecking=no -o ConnectTimeout=5 ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} exit 2>/dev/null; then
              echo "SSH is ready"
              exit 0
            fi
            echo "Attempt $i/10 — Waiting for SSH..."
            sleep 10
          done
          echo "SSH not available after 10 attempts"
          exit 1

      - name: Renew SSL certificates
        run: |
          ssh -o StrictHostKeyChecking=no -o ServerAliveInterval=30 -o ServerAliveCountMax=3 ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo certbot renew --non-interactive -v && sudo systemctl reload nginx"

      - name: Docker cleanup before run
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker stop \$(sudo docker ps -q) 2>/dev/null || true && sudo docker system prune -af"

      - name: Docker run ONLYOFFICE-Docs EE
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker pull onlyoffice/4testing-documentserver-ee:${{ vars.ONLYOFFICE_DEV_VERSION }}.${{ vars.ONLYOFFICE_DEV_BUILD }}
          sudo docker run -itd -p 8080:80 -e WOPI_ENABLED=true --name ds-ee-dev onlyoffice/4testing-documentserver-ee:${{ vars.ONLYOFFICE_DEV_VERSION }}.${{ vars.ONLYOFFICE_DEV_BUILD }}
          EOF

      - name: Wait for DS startup 240
        run: sleep 240

      - name: Add example host to DS ipfilter
        continue-on-error: true
        uses: ./.github/actions/set-ipfilter
        with:
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          allow_hosts: ${{ vars.AWS_DEV_SERVER_DOMAIN }} ${{ vars.AWS_DEV_SERVER_EC2_IP }}

      - name: AWS_S3_FALSE Enable nginx config
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo rm -f /etc/nginx/sites-enabled/* && \
             sudo ln -s /etc/nginx/sites-available/default_proxy-https-to-http.conf /etc/nginx/sites-enabled/default_proxy-https-to-http.conf && \
             sudo nginx -t && sudo systemctl reload nginx"

      - name: AWS_S3_FALSE Configure
        continue-on-error: true
        env:
          AWS_DEV_SERVER_BUCKET_NAME: ${{ secrets.AWS_DEV_SERVER_BUCKET_NAME }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: |
          envsubst '${AWS_DEV_SERVER_BUCKET_NAME} ${AWS_ACCESS_KEY_ID} ${AWS_SECRET_ACCESS_KEY}' \
            < dev-SERVER-checks/aws-s3/useDirectStorageUrls-false/local-production-linux.json \
            > /tmp/local-production-linux.json
          scp -o StrictHostKeyChecking=no /tmp/local-production-linux.json \
            ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }}:~/local-production-linux.json
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker cp ~/local-production-linux.json ds-ee-dev:/etc/onlyoffice/documentserver/local-production-linux.json
          sudo rm ~/local-production-linux.json
          sudo docker exec ds-ee-dev supervisorctl restart ds:converter ds:docservice
          sleep 30
          EOF

      - name: AWS_S3_FALSE Info
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo apt-get install jq -y
          echo "-------------healthcheck-------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck; echo
          echo "-------------DS services-------------"
          sudo docker exec ds-ee-dev supervisorctl status all || true
          echo "---------Installed packages----------"
          sudo docker exec ds-ee-dev bash -c "dpkg -l | grep onlyoffice"
          echo "--------packageType--------"
          sudo docker exec ds-ee-dev curl -s http://127.0.0.1/info/info.json | jq | grep -i -e buildVersion -e buildNumber -e packageType
          echo "-------------WOPI--------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/hosting/discovery | tail -c 150; echo
          EOF

      - name: AWS_S3_FALSE Check healthcheck
        continue-on-error: true
        run: |
          HC=$(curl -s "https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck" || echo "error")
          echo "Healthcheck: $HC"
          echo "HEALTHCHECK_AWS_S3_FALSE=$HC" >> $GITHUB_ENV

      - name: AWS_S3_FALSE Check version
        continue-on-error: true
        run: |
          VERSION=$(ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev dpkg-query -W onlyoffice-documentserver-ee" | awk '{print $2}')
          EXPECTED="${{ vars.ONLYOFFICE_DEV_VERSION }}-${{ vars.ONLYOFFICE_DEV_BUILD }}"
          echo "Version: installed=$VERSION, expected=$EXPECTED"
          if [ "$VERSION" = "$EXPECTED" ]; then
            echo "VERSION_AWS_S3_FALSE_OK=true" >> $GITHUB_ENV
          else
            echo "VERSION_AWS_S3_FALSE_OK=false" >> $GITHUB_ENV
          fi
          echo "VERSION_AWS_S3_FALSE_ACTUAL=$VERSION" >> $GITHUB_ENV

      - name: AWS_S3_FALSE Start ds-example
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev supervisorctl start ds:example"

      - name: Clear puppeteer output
        run: rm -rf Dep.Tests/puppeteer/out

      - name: AWS_S3_FALSE Puppeteer API tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          sed -i -E "s#\"url\": \"https://[^/]+\.teamlab\.info/example\"#\"url\": \"https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/example\"#" ../config_chrome_linux.json
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example

      - name: AWS_S3_FALSE Puppeteer WOPI tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          # vsdx.js WOPI test disabled — feature not finished yet (fails on WOPI provider)
          # python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi

      - name: AWS_S3_FALSE Parse puppeteer results
        if: always()
        run: python3 .github/scripts/parse-puppeteer-results.py --edition AWS_S3_FALSE

      - name: AWS_S3_FALSE Documentserver logs
        continue-on-error: true
        uses: ./.github/actions/ds-logs
        with:
          mode: ssh-docker
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev

      - name: AWS_S3_FALSE Check DS logs for errors
        continue-on-error: true
        uses: ./.github/actions/check-ds-logs
        with:
          mode: ssh-docker-logs
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          env_var: DS_LOG_ERRORS_AWS_S3_FALSE
          label: AWS_S3_FALSE

      - name: AWS_S3_TRUE Docker reInstall
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker stop ds-ee-dev || true
          sudo docker rm ds-ee-dev || true
          sudo docker run -itd -p 8080:80 -e WOPI_ENABLED=true --name ds-ee-dev onlyoffice/4testing-documentserver-ee:${{ vars.ONLYOFFICE_DEV_VERSION }}.${{ vars.ONLYOFFICE_DEV_BUILD }}
          EOF

      - name: Wait for DS startup 240
        run: sleep 240

      - name: Add example host to DS ipfilter
        continue-on-error: true
        uses: ./.github/actions/set-ipfilter
        with:
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          allow_hosts: ${{ vars.AWS_DEV_SERVER_DOMAIN }} ${{ vars.AWS_DEV_SERVER_EC2_IP }}

      - name: AWS_S3_TRUE Enable nginx config
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo rm -f /etc/nginx/sites-enabled/* && \
             sudo ln -s /etc/nginx/sites-available/default_proxy-https-to-http.conf /etc/nginx/sites-enabled/default_proxy-https-to-http.conf && \
             sudo nginx -t && sudo systemctl reload nginx"

      - name: AWS_S3_TRUE Configure
        continue-on-error: true
        env:
          AWS_DEV_SERVER_BUCKET_NAME: ${{ secrets.AWS_DEV_SERVER_BUCKET_NAME }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: |
          envsubst '${AWS_DEV_SERVER_BUCKET_NAME} ${AWS_ACCESS_KEY_ID} ${AWS_SECRET_ACCESS_KEY}' \
            < dev-SERVER-checks/aws-s3/useDirectStorageUrls-true/local-production-linux.json \
            > /tmp/local-production-linux.json
          scp -o StrictHostKeyChecking=no /tmp/local-production-linux.json \
            ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }}:~/local-production-linux.json
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker cp ~/local-production-linux.json ds-ee-dev:/etc/onlyoffice/documentserver/local-production-linux.json
          sudo rm ~/local-production-linux.json
          sudo docker exec ds-ee-dev supervisorctl restart ds:converter ds:docservice
          sleep 30
          EOF

      - name: AWS_S3_TRUE Info
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo apt-get install jq -y
          echo "-------------healthcheck-------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck; echo
          echo "-------------DS services-------------"
          sudo docker exec ds-ee-dev supervisorctl status all || true
          echo "---------Installed packages----------"
          sudo docker exec ds-ee-dev bash -c "dpkg -l | grep onlyoffice"
          echo "--------packageType--------"
          sudo docker exec ds-ee-dev curl -s http://127.0.0.1/info/info.json | jq | grep -i -e buildVersion -e buildNumber -e packageType
          echo "-------------WOPI--------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/hosting/discovery | tail -c 150; echo
          EOF

      - name: AWS_S3_TRUE Check healthcheck
        continue-on-error: true
        run: |
          HC=$(curl -s "https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck" || echo "error")
          echo "Healthcheck: $HC"
          echo "HEALTHCHECK_AWS_S3_TRUE=$HC" >> $GITHUB_ENV

      - name: AWS_S3_TRUE Check version
        continue-on-error: true
        run: |
          VERSION=$(ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev dpkg-query -W onlyoffice-documentserver-ee" | awk '{print $2}')
          EXPECTED="${{ vars.ONLYOFFICE_DEV_VERSION }}-${{ vars.ONLYOFFICE_DEV_BUILD }}"
          echo "Version: installed=$VERSION, expected=$EXPECTED"
          if [ "$VERSION" = "$EXPECTED" ]; then
            echo "VERSION_AWS_S3_TRUE_OK=true" >> $GITHUB_ENV
          else
            echo "VERSION_AWS_S3_TRUE_OK=false" >> $GITHUB_ENV
          fi
          echo "VERSION_AWS_S3_TRUE_ACTUAL=$VERSION" >> $GITHUB_ENV

      - name: AWS_S3_TRUE Start ds-example
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev supervisorctl start ds:example"

      - name: Clear puppeteer output
        run: rm -rf Dep.Tests/puppeteer/out

      - name: AWS_S3_TRUE Puppeteer API tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example

      - name: AWS_S3_TRUE Puppeteer WOPI tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          # vsdx.js WOPI test disabled — feature not finished yet (fails on WOPI provider)
          # python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi

      - name: AWS_S3_TRUE Parse puppeteer results
        if: always()
        run: python3 .github/scripts/parse-puppeteer-results.py --edition AWS_S3_TRUE

      - name: AWS_S3_TRUE Documentserver logs
        continue-on-error: true
        uses: ./.github/actions/ds-logs
        with:
          mode: ssh-docker
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev

      - name: AWS_S3_TRUE Check DS logs for errors
        continue-on-error: true
        uses: ./.github/actions/check-ds-logs
        with:
          mode: ssh-docker-logs
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          env_var: DS_LOG_ERRORS_AWS_S3_TRUE
          label: AWS_S3_TRUE

      - name: AWS_S3_PATH-STYLE Docker reInstall
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker stop ds-ee-dev || true
          sudo docker rm ds-ee-dev || true
          sudo docker run -itd -p 8080:80 -e WOPI_ENABLED=true --name ds-ee-dev onlyoffice/4testing-documentserver-ee:${{ vars.ONLYOFFICE_DEV_VERSION }}.${{ vars.ONLYOFFICE_DEV_BUILD }}
          EOF

      - name: Wait for DS startup 240
        run: sleep 240

      - name: Add example host to DS ipfilter
        continue-on-error: true
        uses: ./.github/actions/set-ipfilter
        with:
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          allow_hosts: ${{ vars.AWS_DEV_SERVER_DOMAIN }} ${{ vars.AWS_DEV_SERVER_EC2_IP }}

      - name: AWS_S3_PATH-STYLE Enable nginx config
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo rm -f /etc/nginx/sites-enabled/* && \
             sudo ln -s /etc/nginx/sites-available/default_proxy-https-to-http.conf /etc/nginx/sites-enabled/default_proxy-https-to-http.conf && \
             sudo nginx -t && sudo systemctl reload nginx"

      - name: AWS_S3_PATH-STYLE Configure
        continue-on-error: true
        env:
          AWS_DEV_SERVER_BUCKET_NAME: ${{ secrets.AWS_DEV_SERVER_BUCKET_NAME }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: |
          envsubst '${AWS_DEV_SERVER_BUCKET_NAME} ${AWS_ACCESS_KEY_ID} ${AWS_SECRET_ACCESS_KEY}' \
            < dev-SERVER-checks/aws-s3/s3ForcePathStyle-true/local-production-linux.json \
            > /tmp/local-production-linux.json
          scp -o StrictHostKeyChecking=no /tmp/local-production-linux.json \
            ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }}:~/local-production-linux.json
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker cp ~/local-production-linux.json ds-ee-dev:/etc/onlyoffice/documentserver/local-production-linux.json
          sudo rm ~/local-production-linux.json
          sudo docker exec ds-ee-dev supervisorctl restart ds:converter ds:docservice
          sleep 30
          EOF

      - name: AWS_S3_PATH-STYLE Info
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo apt-get install jq -y
          echo "-------------healthcheck-------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck; echo
          echo "-------------DS services-------------"
          sudo docker exec ds-ee-dev supervisorctl status all || true
          echo "---------Installed packages----------"
          sudo docker exec ds-ee-dev bash -c "dpkg -l | grep onlyoffice"
          echo "--------packageType--------"
          sudo docker exec ds-ee-dev curl -s http://127.0.0.1/info/info.json | jq | grep -i -e buildVersion -e buildNumber -e packageType
          echo "-------------WOPI--------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/hosting/discovery | tail -c 150; echo
          EOF

      - name: AWS_S3_PATH-STYLE Check healthcheck
        continue-on-error: true
        run: |
          HC=$(curl -s "https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck" || echo "error")
          echo "Healthcheck: $HC"
          echo "HEALTHCHECK_AWS_S3_PATH_STYLE=$HC" >> $GITHUB_ENV

      - name: AWS_S3_PATH-STYLE Check version
        continue-on-error: true
        run: |
          VERSION=$(ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev dpkg-query -W onlyoffice-documentserver-ee" | awk '{print $2}')
          EXPECTED="${{ vars.ONLYOFFICE_DEV_VERSION }}-${{ vars.ONLYOFFICE_DEV_BUILD }}"
          echo "Version: installed=$VERSION, expected=$EXPECTED"
          if [ "$VERSION" = "$EXPECTED" ]; then
            echo "VERSION_AWS_S3_PATH_STYLE_OK=true" >> $GITHUB_ENV
          else
            echo "VERSION_AWS_S3_PATH_STYLE_OK=false" >> $GITHUB_ENV
          fi
          echo "VERSION_AWS_S3_PATH_STYLE_ACTUAL=$VERSION" >> $GITHUB_ENV

      - name: AWS_S3_PATH-STYLE Start ds-example
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev supervisorctl start ds:example"

      - name: Clear puppeteer output
        run: rm -rf Dep.Tests/puppeteer/out

      - name: AWS_S3_PATH-STYLE Puppeteer API tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example

      - name: AWS_S3_PATH-STYLE Puppeteer WOPI tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          # vsdx.js WOPI test disabled — feature not finished yet (fails on WOPI provider)
          # python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi

      - name: AWS_S3_PATH-STYLE Parse puppeteer results
        if: always()
        run: python3 .github/scripts/parse-puppeteer-results.py --edition AWS_S3_PATH_STYLE

      - name: AWS_S3_PATH-STYLE Documentserver logs
        continue-on-error: true
        uses: ./.github/actions/ds-logs
        with:
          mode: ssh-docker
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev

      - name: AWS_S3_PATH-STYLE Check DS logs for errors
        continue-on-error: true
        uses: ./.github/actions/check-ds-logs
        with:
          mode: ssh-docker-logs
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          env_var: DS_LOG_ERRORS_AWS_S3_PATH_STYLE
          label: AWS_S3_PATH-STYLE

      - name: AWS_S3_KMS Docker reInstall
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker stop ds-ee-dev || true
          sudo docker rm ds-ee-dev || true
          sudo docker run -itd -p 8080:80 -e WOPI_ENABLED=true --name ds-ee-dev onlyoffice/4testing-documentserver-ee:${{ vars.ONLYOFFICE_DEV_VERSION }}.${{ vars.ONLYOFFICE_DEV_BUILD }}
          EOF

      - name: Wait for DS startup 240
        run: sleep 240

      - name: Add example host to DS ipfilter
        continue-on-error: true
        uses: ./.github/actions/set-ipfilter
        with:
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          allow_hosts: ${{ vars.AWS_DEV_SERVER_DOMAIN }} ${{ vars.AWS_DEV_SERVER_EC2_IP }}

      - name: AWS_S3_KMS Enable nginx config
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo rm -f /etc/nginx/sites-enabled/* && \
             sudo ln -s /etc/nginx/sites-available/default_proxy-https-to-http.conf /etc/nginx/sites-enabled/default_proxy-https-to-http.conf && \
             sudo nginx -t && sudo systemctl reload nginx"

      - name: AWS_S3_KMS Configure
        continue-on-error: true
        env:
          AWS_DEV_SERVER_BUCKET_NAME: ${{ secrets.AWS_DEV_SERVER_BUCKET_NAME }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_KMS_KEY_ID: ${{ secrets.AWS_KMS_KEY_ID }}
        run: |
          envsubst '${AWS_DEV_SERVER_BUCKET_NAME} ${AWS_ACCESS_KEY_ID} ${AWS_SECRET_ACCESS_KEY} ${AWS_KMS_KEY_ID}' \
            < dev-SERVER-checks/aws-s3/aws-kms/local-production-linux.json \
            > /tmp/local-production-linux.json
          scp -o StrictHostKeyChecking=no /tmp/local-production-linux.json \
            ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }}:~/local-production-linux.json
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker cp ~/local-production-linux.json ds-ee-dev:/etc/onlyoffice/documentserver/local-production-linux.json
          sudo rm ~/local-production-linux.json
          sudo docker exec ds-ee-dev supervisorctl restart ds:converter ds:docservice
          sleep 30
          EOF

      - name: AWS_S3_KMS Info
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo apt-get install jq -y
          echo "-------------healthcheck-------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck; echo
          echo "-------------DS services-------------"
          sudo docker exec ds-ee-dev supervisorctl status all || true
          echo "---------Installed packages----------"
          sudo docker exec ds-ee-dev bash -c "dpkg -l | grep onlyoffice"
          echo "--------packageType--------"
          sudo docker exec ds-ee-dev curl -s http://127.0.0.1/info/info.json | jq | grep -i -e buildVersion -e buildNumber -e packageType
          echo "-------------WOPI--------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/hosting/discovery | tail -c 150; echo
          EOF

      - name: AWS_S3_KMS Check healthcheck
        continue-on-error: true
        run: |
          HC=$(curl -s "https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck" || echo "error")
          echo "Healthcheck: $HC"
          echo "HEALTHCHECK_AWS_S3_KMS=$HC" >> $GITHUB_ENV

      - name: AWS_S3_KMS Check version
        continue-on-error: true
        run: |
          VERSION=$(ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev dpkg-query -W onlyoffice-documentserver-ee" | awk '{print $2}')
          EXPECTED="${{ vars.ONLYOFFICE_DEV_VERSION }}-${{ vars.ONLYOFFICE_DEV_BUILD }}"
          echo "Version: installed=$VERSION, expected=$EXPECTED"
          if [ "$VERSION" = "$EXPECTED" ]; then
            echo "VERSION_AWS_S3_KMS_OK=true" >> $GITHUB_ENV
          else
            echo "VERSION_AWS_S3_KMS_OK=false" >> $GITHUB_ENV
          fi
          echo "VERSION_AWS_S3_KMS_ACTUAL=$VERSION" >> $GITHUB_ENV

      - name: AWS_S3_KMS Start ds-example
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev supervisorctl start ds:example"

      - name: Clear puppeteer output
        run: rm -rf Dep.Tests/puppeteer/out

      - name: AWS_S3_KMS Puppeteer API tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example

      - name: AWS_S3_KMS Puppeteer WOPI tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          # vsdx.js WOPI test disabled — feature not finished yet (fails on WOPI provider)
          # python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi

      - name: AWS_S3_KMS Parse puppeteer results
        if: always()
        run: python3 .github/scripts/parse-puppeteer-results.py --edition AWS_S3_KMS

      - name: AWS_S3_KMS Documentserver logs
        continue-on-error: true
        uses: ./.github/actions/ds-logs
        with:
          mode: ssh-docker
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev

      - name: AWS_S3_KMS Check DS logs for errors
        continue-on-error: true
        uses: ./.github/actions/check-ds-logs
        with:
          mode: ssh-docker-logs
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          env_var: DS_LOG_ERRORS_AWS_S3_KMS
          label: AWS_S3_KMS

      - name: AZURE_STORAGE_DIRECTURL_FALSE Docker reInstall
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker stop ds-ee-dev || true
          sudo docker rm ds-ee-dev || true
          sudo docker run -itd -p 8080:80 -e WOPI_ENABLED=true --name ds-ee-dev onlyoffice/4testing-documentserver-ee:${{ vars.ONLYOFFICE_DEV_VERSION }}.${{ vars.ONLYOFFICE_DEV_BUILD }}
          EOF

      - name: Wait for DS startup 240
        run: sleep 240

      - name: Add example host to DS ipfilter
        continue-on-error: true
        uses: ./.github/actions/set-ipfilter
        with:
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          allow_hosts: ${{ vars.AWS_DEV_SERVER_DOMAIN }} ${{ vars.AWS_DEV_SERVER_EC2_IP }}

      - name: AZURE_STORAGE_DIRECTURL_FALSE Enable nginx config
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo rm -f /etc/nginx/sites-enabled/* && \
             sudo ln -s /etc/nginx/sites-available/default_proxy-https-to-http.conf /etc/nginx/sites-enabled/default_proxy-https-to-http.conf && \
             sudo nginx -t && sudo systemctl reload nginx"

      - name: AZURE_STORAGE_DIRECTURL_FALSE Configure
        continue-on-error: true
        env:
          AZURE_STORAGE_KEY_ID: ${{ secrets.AZURE_STORAGE_KEY_ID }}
          AZURE_STORAGE_ACCESS_KEY: ${{ secrets.AZURE_STORAGE_ACCESS_KEY }}
        run: |
          envsubst '${AZURE_STORAGE_KEY_ID} ${AZURE_STORAGE_ACCESS_KEY}' \
            < dev-SERVER-checks/azure-blob-storage/useDirectStorageUrls-false/local-production-linux.json \
            > /tmp/local-production-linux.json
          scp -o StrictHostKeyChecking=no /tmp/local-production-linux.json \
            ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }}:~/local-production-linux.json
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker cp ~/local-production-linux.json ds-ee-dev:/etc/onlyoffice/documentserver/local-production-linux.json
          sudo rm ~/local-production-linux.json
          sudo docker exec ds-ee-dev supervisorctl restart ds:converter ds:docservice
          sleep 30
          EOF

      - name: AZURE_STORAGE_DIRECTURL_FALSE Info
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo apt-get install jq -y
          echo "-------------healthcheck-------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck; echo
          echo "-------------DS services-------------"
          sudo docker exec ds-ee-dev supervisorctl status all || true
          echo "---------Installed packages----------"
          sudo docker exec ds-ee-dev bash -c "dpkg -l | grep onlyoffice"
          echo "--------packageType--------"
          sudo docker exec ds-ee-dev curl -s http://127.0.0.1/info/info.json | jq | grep -i -e buildVersion -e buildNumber -e packageType
          echo "-------------WOPI--------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/hosting/discovery | tail -c 150; echo
          EOF

      - name: AZURE_STORAGE_DIRECTURL_FALSE Check healthcheck
        continue-on-error: true
        run: |
          HC=$(curl -s "https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck" || echo "error")
          echo "Healthcheck: $HC"
          echo "HEALTHCHECK_AZURE_STORAGE_DIRECTURL_FALSE=$HC" >> $GITHUB_ENV

      - name: AZURE_STORAGE_DIRECTURL_FALSE Check version
        continue-on-error: true
        run: |
          VERSION=$(ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev dpkg-query -W onlyoffice-documentserver-ee" | awk '{print $2}')
          EXPECTED="${{ vars.ONLYOFFICE_DEV_VERSION }}-${{ vars.ONLYOFFICE_DEV_BUILD }}"
          echo "Version: installed=$VERSION, expected=$EXPECTED"
          if [ "$VERSION" = "$EXPECTED" ]; then
            echo "VERSION_AZURE_STORAGE_DIRECTURL_FALSE_OK=true" >> $GITHUB_ENV
          else
            echo "VERSION_AZURE_STORAGE_DIRECTURL_FALSE_OK=false" >> $GITHUB_ENV
          fi
          echo "VERSION_AZURE_STORAGE_DIRECTURL_FALSE_ACTUAL=$VERSION" >> $GITHUB_ENV

      - name: AZURE_STORAGE_DIRECTURL_FALSE Start ds-example
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev supervisorctl start ds:example"

      - name: Clear puppeteer output
        run: rm -rf Dep.Tests/puppeteer/out

      - name: AZURE_STORAGE_DIRECTURL_FALSE Puppeteer API tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example

      - name: AZURE_STORAGE_DIRECTURL_FALSE Puppeteer WOPI tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          # vsdx.js WOPI test disabled — feature not finished yet (fails on WOPI provider)
          # python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi

      - name: AZURE_STORAGE_DIRECTURL_FALSE Parse puppeteer results
        if: always()
        run: python3 .github/scripts/parse-puppeteer-results.py --edition AZURE_STORAGE_DIRECTURL_FALSE

      - name: AZURE_STORAGE_DIRECTURL_FALSE Documentserver logs
        continue-on-error: true
        uses: ./.github/actions/ds-logs
        with:
          mode: ssh-docker
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev

      - name: AZURE_STORAGE_DIRECTURL_FALSE Check DS logs for errors
        continue-on-error: true
        uses: ./.github/actions/check-ds-logs
        with:
          mode: ssh-docker-logs
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          env_var: DS_LOG_ERRORS_AZURE_STORAGE_DIRECTURL_FALSE
          label: AZURE_STORAGE_DIRECTURL_FALSE

      - name: AZURE_STORAGE_DIRECTURL_TRUE Docker reInstall
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker stop ds-ee-dev || true
          sudo docker rm ds-ee-dev || true
          sudo docker run -itd -p 8080:80 -e WOPI_ENABLED=true --name ds-ee-dev onlyoffice/4testing-documentserver-ee:${{ vars.ONLYOFFICE_DEV_VERSION }}.${{ vars.ONLYOFFICE_DEV_BUILD }}
          EOF

      - name: Wait for DS startup 240
        run: sleep 240

      - name: Add example host to DS ipfilter
        continue-on-error: true
        uses: ./.github/actions/set-ipfilter
        with:
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          allow_hosts: ${{ vars.AWS_DEV_SERVER_DOMAIN }} ${{ vars.AWS_DEV_SERVER_EC2_IP }}

      - name: AZURE_STORAGE_DIRECTURL_TRUE Enable nginx config
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo rm -f /etc/nginx/sites-enabled/* && \
             sudo ln -s /etc/nginx/sites-available/default_proxy-https-to-http.conf /etc/nginx/sites-enabled/default_proxy-https-to-http.conf && \
             sudo nginx -t && sudo systemctl reload nginx"

      - name: AZURE_STORAGE_DIRECTURL_TRUE Configure
        continue-on-error: true
        env:
          AZURE_STORAGE_KEY_ID: ${{ secrets.AZURE_STORAGE_KEY_ID }}
          AZURE_STORAGE_ACCESS_KEY: ${{ secrets.AZURE_STORAGE_ACCESS_KEY }}
        run: |
          envsubst '${AZURE_STORAGE_KEY_ID} ${AZURE_STORAGE_ACCESS_KEY}' \
            < dev-SERVER-checks/azure-blob-storage/useDirectStorageUrls-true/local-production-linux.json \
            > /tmp/local-production-linux.json
          scp -o StrictHostKeyChecking=no /tmp/local-production-linux.json \
            ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }}:~/local-production-linux.json
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker cp ~/local-production-linux.json ds-ee-dev:/etc/onlyoffice/documentserver/local-production-linux.json
          sudo rm ~/local-production-linux.json
          sudo docker exec ds-ee-dev supervisorctl restart ds:converter ds:docservice
          sleep 30
          EOF

      - name: AZURE_STORAGE_DIRECTURL_TRUE Info
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo apt-get install jq -y
          echo "-------------healthcheck-------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck; echo
          echo "-------------DS services-------------"
          sudo docker exec ds-ee-dev supervisorctl status all || true
          echo "---------Installed packages----------"
          sudo docker exec ds-ee-dev bash -c "dpkg -l | grep onlyoffice"
          echo "--------packageType--------"
          sudo docker exec ds-ee-dev curl -s http://127.0.0.1/info/info.json | jq | grep -i -e buildVersion -e buildNumber -e packageType
          echo "-------------WOPI--------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/hosting/discovery | tail -c 150; echo
          EOF

      - name: AZURE_STORAGE_DIRECTURL_TRUE Check healthcheck
        continue-on-error: true
        run: |
          HC=$(curl -s "https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck" || echo "error")
          echo "Healthcheck: $HC"
          echo "HEALTHCHECK_AZURE_STORAGE_DIRECTURL_TRUE=$HC" >> $GITHUB_ENV

      - name: AZURE_STORAGE_DIRECTURL_TRUE Check version
        continue-on-error: true
        run: |
          VERSION=$(ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev dpkg-query -W onlyoffice-documentserver-ee" | awk '{print $2}')
          EXPECTED="${{ vars.ONLYOFFICE_DEV_VERSION }}-${{ vars.ONLYOFFICE_DEV_BUILD }}"
          echo "Version: installed=$VERSION, expected=$EXPECTED"
          if [ "$VERSION" = "$EXPECTED" ]; then
            echo "VERSION_AZURE_STORAGE_DIRECTURL_TRUE_OK=true" >> $GITHUB_ENV
          else
            echo "VERSION_AZURE_STORAGE_DIRECTURL_TRUE_OK=false" >> $GITHUB_ENV
          fi
          echo "VERSION_AZURE_STORAGE_DIRECTURL_TRUE_ACTUAL=$VERSION" >> $GITHUB_ENV

      - name: AZURE_STORAGE_DIRECTURL_TRUE Start ds-example
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev supervisorctl start ds:example"

      - name: Clear puppeteer output
        run: rm -rf Dep.Tests/puppeteer/out

      - name: AZURE_STORAGE_DIRECTURL_TRUE Puppeteer API tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example

      - name: AZURE_STORAGE_DIRECTURL_TRUE Puppeteer WOPI tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          # vsdx.js WOPI test disabled — feature not finished yet (fails on WOPI provider)
          # python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi

      - name: AZURE_STORAGE_DIRECTURL_TRUE Parse puppeteer results
        if: always()
        run: python3 .github/scripts/parse-puppeteer-results.py --edition AZURE_STORAGE_DIRECTURL_TRUE

      - name: AZURE_STORAGE_DIRECTURL_TRUE Documentserver logs
        continue-on-error: true
        uses: ./.github/actions/ds-logs
        with:
          mode: ssh-docker
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev

      - name: AZURE_STORAGE_DIRECTURL_TRUE Check DS logs for errors
        continue-on-error: true
        uses: ./.github/actions/check-ds-logs
        with:
          mode: ssh-docker-logs
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          env_var: DS_LOG_ERRORS_AZURE_STORAGE_DIRECTURL_TRUE
          label: AZURE_STORAGE_DIRECTURL_TRUE

      - name: AZURE_STORAGE_ENCRYPTION_SCOPE Docker reInstall
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker stop ds-ee-dev || true
          sudo docker rm ds-ee-dev || true
          sudo docker run -itd -p 8080:80 -e WOPI_ENABLED=true --name ds-ee-dev onlyoffice/4testing-documentserver-ee:${{ vars.ONLYOFFICE_DEV_VERSION }}.${{ vars.ONLYOFFICE_DEV_BUILD }}
          EOF

      - name: Wait for DS startup 240
        run: sleep 240

      - name: Add example host to DS ipfilter
        continue-on-error: true
        uses: ./.github/actions/set-ipfilter
        with:
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          allow_hosts: ${{ vars.AWS_DEV_SERVER_DOMAIN }} ${{ vars.AWS_DEV_SERVER_EC2_IP }}

      - name: AZURE_STORAGE_ENCRYPTION_SCOPE Enable nginx config
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo rm -f /etc/nginx/sites-enabled/* && \
             sudo ln -s /etc/nginx/sites-available/default_proxy-https-to-http.conf /etc/nginx/sites-enabled/default_proxy-https-to-http.conf && \
             sudo nginx -t && sudo systemctl reload nginx"

      - name: AZURE_STORAGE_ENCRYPTION_SCOPE Configure
        continue-on-error: true
        env:
          AZURE_STORAGE_KEY_ID: ${{ secrets.AZURE_STORAGE_KEY_ID }}
          AZURE_STORAGE_ACCESS_KEY: ${{ secrets.AZURE_STORAGE_ACCESS_KEY }}
          AZURE_STORAGE_ENCRYPTION_SCOPE: ${{ secrets.AZURE_STORAGE_ENCRYPTION_SCOPE }}
        run: |
          envsubst '${AZURE_STORAGE_KEY_ID} ${AZURE_STORAGE_ACCESS_KEY} ${AZURE_STORAGE_ENCRYPTION_SCOPE}' \
            < dev-SERVER-checks/azure-blob-storage/az-encryptionScope/local-production-linux.json \
            > /tmp/local-production-linux.json
          scp -o StrictHostKeyChecking=no /tmp/local-production-linux.json \
            ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }}:~/local-production-linux.json
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker cp ~/local-production-linux.json ds-ee-dev:/etc/onlyoffice/documentserver/local-production-linux.json
          sudo rm ~/local-production-linux.json
          sudo docker exec ds-ee-dev supervisorctl restart ds:converter ds:docservice
          sleep 30
          EOF

      - name: AZURE_STORAGE_ENCRYPTION_SCOPE Info
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo apt-get install jq -y
          echo "-------------healthcheck-------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck; echo
          echo "-------------DS services-------------"
          sudo docker exec ds-ee-dev supervisorctl status all || true
          echo "---------Installed packages----------"
          sudo docker exec ds-ee-dev bash -c "dpkg -l | grep onlyoffice"
          echo "--------packageType--------"
          sudo docker exec ds-ee-dev curl -s http://127.0.0.1/info/info.json | jq | grep -i -e buildVersion -e buildNumber -e packageType
          echo "-------------WOPI--------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/hosting/discovery | tail -c 150; echo
          EOF

      - name: AZURE_STORAGE_ENCRYPTION_SCOPE Check healthcheck
        continue-on-error: true
        run: |
          HC=$(curl -s "https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck" || echo "error")
          echo "Healthcheck: $HC"
          echo "HEALTHCHECK_AZURE_STORAGE_ENCRYPTION_SCOPE=$HC" >> $GITHUB_ENV

      - name: AZURE_STORAGE_ENCRYPTION_SCOPE Check version
        continue-on-error: true
        run: |
          VERSION=$(ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev dpkg-query -W onlyoffice-documentserver-ee" | awk '{print $2}')
          EXPECTED="${{ vars.ONLYOFFICE_DEV_VERSION }}-${{ vars.ONLYOFFICE_DEV_BUILD }}"
          echo "Version: installed=$VERSION, expected=$EXPECTED"
          if [ "$VERSION" = "$EXPECTED" ]; then
            echo "VERSION_AZURE_STORAGE_ENCRYPTION_SCOPE_OK=true" >> $GITHUB_ENV
          else
            echo "VERSION_AZURE_STORAGE_ENCRYPTION_SCOPE_OK=false" >> $GITHUB_ENV
          fi
          echo "VERSION_AZURE_STORAGE_ENCRYPTION_SCOPE_ACTUAL=$VERSION" >> $GITHUB_ENV

      - name: AZURE_STORAGE_ENCRYPTION_SCOPE Start ds-example
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev supervisorctl start ds:example"

      - name: Clear puppeteer output
        run: rm -rf Dep.Tests/puppeteer/out

      - name: AZURE_STORAGE_ENCRYPTION_SCOPE Puppeteer API tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example

      - name: AZURE_STORAGE_ENCRYPTION_SCOPE Puppeteer WOPI tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          # vsdx.js WOPI test disabled — feature not finished yet (fails on WOPI provider)
          # python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi

      - name: AZURE_STORAGE_ENCRYPTION_SCOPE Parse puppeteer results
        if: always()
        run: python3 .github/scripts/parse-puppeteer-results.py --edition AZURE_STORAGE_ENCRYPTION_SCOPE

      - name: AZURE_STORAGE_ENCRYPTION_SCOPE Documentserver logs
        continue-on-error: true
        uses: ./.github/actions/ds-logs
        with:
          mode: ssh-docker
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev

      - name: AZURE_STORAGE_ENCRYPTION_SCOPE Check DS logs for errors
        continue-on-error: true
        uses: ./.github/actions/check-ds-logs
        with:
          mode: ssh-docker-logs
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          env_var: DS_LOG_ERRORS_AZURE_STORAGE_ENCRYPTION_SCOPE
          label: AZURE_STORAGE_ENCRYPTION_SCOPE

      - name: DS-VPATH Docker reInstall
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker stop ds-ee-dev || true
          sudo docker rm ds-ee-dev || true
          sudo docker run -itd -p 8080:80 -e WOPI_ENABLED=true --name ds-ee-dev onlyoffice/4testing-documentserver-ee:${{ vars.ONLYOFFICE_DEV_VERSION }}.${{ vars.ONLYOFFICE_DEV_BUILD }}
          EOF

      - name: Wait for DS startup 240
        run: sleep 240

      - name: Add example host to DS ipfilter
        continue-on-error: true
        uses: ./.github/actions/set-ipfilter
        with:
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          allow_hosts: ${{ vars.AWS_DEV_SERVER_DOMAIN }} ${{ vars.AWS_DEV_SERVER_EC2_IP }}

      - name: DS-VPATH Enable nginx config
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo rm -f /etc/nginx/sites-enabled/* && \
             sudo ln -s /etc/nginx/sites-available/default_proxy-to-virtual-path.conf /etc/nginx/sites-enabled/default_proxy-to-virtual-path.conf && \
             sudo nginx -t && sudo systemctl reload nginx"

      - name: DS-VPATH Info
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo apt-get install jq -y
          echo "-------------healthcheck-------------"
          curl -s http://${{ vars.AWS_DEV_SERVER_EC2_IP }}/ds-vpath/healthcheck; echo
          echo "-------------DS services-------------"
          sudo docker exec ds-ee-dev supervisorctl status all || true
          echo "---------Installed packages----------"
          sudo docker exec ds-ee-dev bash -c "dpkg -l | grep onlyoffice"
          echo "--------packageType--------"
          sudo docker exec ds-ee-dev curl -s http://127.0.0.1/info/info.json | jq | grep -i -e buildVersion -e buildNumber -e packageType
          echo "-------------WOPI--------------"
          curl -s http://${{ vars.AWS_DEV_SERVER_EC2_IP }}/ds-vpath/hosting/discovery | tail -c 150; echo
          EOF

      - name: DS-VPATH Check healthcheck
        continue-on-error: true
        run: |
          HC=$(curl -s "http://${{ vars.AWS_DEV_SERVER_EC2_IP }}/ds-vpath/healthcheck" || echo "error")
          echo "Healthcheck: $HC"
          echo "HEALTHCHECK_DS_VPATH=$HC" >> $GITHUB_ENV

      - name: DS-VPATH Check version
        continue-on-error: true
        run: |
          VERSION=$(ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev dpkg-query -W onlyoffice-documentserver-ee" | awk '{print $2}')
          EXPECTED="${{ vars.ONLYOFFICE_DEV_VERSION }}-${{ vars.ONLYOFFICE_DEV_BUILD }}"
          echo "Version: installed=$VERSION, expected=$EXPECTED"
          if [ "$VERSION" = "$EXPECTED" ]; then
            echo "VERSION_DS_VPATH_OK=true" >> $GITHUB_ENV
          else
            echo "VERSION_DS_VPATH_OK=false" >> $GITHUB_ENV
          fi
          echo "VERSION_DS_VPATH_ACTUAL=$VERSION" >> $GITHUB_ENV

      - name: DS-VPATH Start ds-example
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev supervisorctl start ds:example"

      - name: DS-VPATH Clear puppeteer output
        run: rm -rf Dep.Tests/puppeteer/out

      - name: DS-VPATH Puppeteer API tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          sed -i -E "s#\"url\": \"https://[^\"]*/example\"#\"url\": \"http://${{ vars.AWS_DEV_SERVER_EC2_IP }}/ds-vpath/example\"#" ../config_chrome_linux.json
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example

      - name: DS-VPATH Puppeteer WOPI tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          # vsdx.js WOPI test disabled — feature not finished yet (fails on WOPI provider)
          # python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi

      - name: DS-VPATH Parse puppeteer results
        if: always()
        run: python3 .github/scripts/parse-puppeteer-results.py --edition DS_VPATH

      - name: DS-VPATH Documentserver logs
        continue-on-error: true
        uses: ./.github/actions/ds-logs
        with:
          mode: ssh-docker
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev

      - name: DS-VPATH Check DS logs for errors
        continue-on-error: true
        uses: ./.github/actions/check-ds-logs
        with:
          mode: ssh-docker-logs
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          env_var: DS_LOG_ERRORS_DS_VPATH
          label: DS_VPATH

      - name: AMQP_ARTEMIS Docker reInstall
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker stop \$(sudo docker ps -q) 2>/dev/null || true
          sudo docker rm \$(sudo docker ps -aq) 2>/dev/null || true
          sudo docker network rm ds-network 2>/dev/null || true
          sudo docker network create ds-network
          sudo docker run -d --network ds-network \
            --name onlyoffice-mq \
            -e ARTEMIS_USER=artemis \
            -e ARTEMIS_PASSWORD=artemis \
            apache/activemq-artemis:2.40.0-alpine
          sudo docker run -itd --network ds-network -p 8080:80 \
            --name ds-ee-dev \
            -e AMQP_TYPE=activemq \
            -e AMQP_URI=amqp://artemis:artemis@onlyoffice-mq \
            -e JWT_ENABLED=true \
            -e JWT_SECRET=MYSECRET \
            -e WOPI_ENABLED=true \
            onlyoffice/4testing-documentserver-ee:${{ vars.ONLYOFFICE_DEV_VERSION }}.${{ vars.ONLYOFFICE_DEV_BUILD }}
          EOF

      - name: Wait for DS startup 240
        run: sleep 240

      - name: Add example host to DS ipfilter
        continue-on-error: true
        uses: ./.github/actions/set-ipfilter
        with:
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          allow_hosts: ${{ vars.AWS_DEV_SERVER_DOMAIN }} ${{ vars.AWS_DEV_SERVER_EC2_IP }}

      - name: AMQP_ARTEMIS Enable nginx config
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo rm -f /etc/nginx/sites-enabled/* && \
             sudo ln -s /etc/nginx/sites-available/default_proxy-https-to-http.conf /etc/nginx/sites-enabled/default_proxy-https-to-http.conf && \
             sudo nginx -t && sudo systemctl reload nginx"

      - name: AMQP_ARTEMIS Info
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo apt-get install jq -y
          echo "-------------healthcheck-------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck; echo
          echo "-------------DS services-------------"
          sudo docker exec ds-ee-dev supervisorctl status all || true
          echo "---------Installed packages----------"
          sudo docker exec ds-ee-dev bash -c "dpkg -l | grep onlyoffice"
          echo "--------packageType--------"
          sudo docker exec ds-ee-dev curl -s http://127.0.0.1/info/info.json | jq | grep -i -e buildVersion -e buildNumber -e packageType
          echo "-------------WOPI--------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/hosting/discovery | tail -c 150; echo
          EOF

      - name: AMQP_ARTEMIS Check healthcheck
        continue-on-error: true
        run: |
          HC=$(curl -s "https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck" || echo "error")
          echo "Healthcheck: $HC"
          echo "HEALTHCHECK_AMQP_ARTEMIS=$HC" >> $GITHUB_ENV

      - name: AMQP_ARTEMIS Check version
        continue-on-error: true
        run: |
          VERSION=$(ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev dpkg-query -W onlyoffice-documentserver-ee" | awk '{print $2}')
          EXPECTED="${{ vars.ONLYOFFICE_DEV_VERSION }}-${{ vars.ONLYOFFICE_DEV_BUILD }}"
          echo "Version: installed=$VERSION, expected=$EXPECTED"
          if [ "$VERSION" = "$EXPECTED" ]; then
            echo "VERSION_AMQP_ARTEMIS_OK=true" >> $GITHUB_ENV
          else
            echo "VERSION_AMQP_ARTEMIS_OK=false" >> $GITHUB_ENV
          fi
          echo "VERSION_AMQP_ARTEMIS_ACTUAL=$VERSION" >> $GITHUB_ENV

      - name: AMQP_ARTEMIS Start ds-example
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev supervisorctl start ds:example"

      - name: AMQP_ARTEMIS Clear puppeteer output
        run: rm -rf Dep.Tests/puppeteer/out

      - name: AMQP_ARTEMIS Puppeteer API tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          sed -i -E "s#\"url\": \"[^\"]+/example\"#\"url\": \"https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/example\"#" ../config_chrome_linux.json
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example

      - name: AMQP_ARTEMIS Puppeteer WOPI tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          # vsdx.js WOPI test disabled — feature not finished yet (fails on WOPI provider)
          # python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi

      - name: AMQP_ARTEMIS Parse puppeteer results
        if: always()
        run: python3 .github/scripts/parse-puppeteer-results.py --edition AMQP_ARTEMIS

      - name: AMQP_ARTEMIS Documentserver logs
        continue-on-error: true
        uses: ./.github/actions/ds-logs
        with:
          mode: ssh-docker
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev

      - name: AMQP_ARTEMIS Check DS logs for errors
        continue-on-error: true
        uses: ./.github/actions/check-ds-logs
        with:
          mode: ssh-docker-logs
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          env_var: DS_LOG_ERRORS_AMQP_ARTEMIS
          label: AMQP_ARTEMIS

      - name: AMQP_CLASSIC Docker reInstall
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker stop ds-ee-dev onlyoffice-mq || true
          sudo docker rm ds-ee-dev onlyoffice-mq || true
          sudo docker run -d --network ds-network \
            --name onlyoffice-mq \
            -e ACTIVEMQ_USERNAME=admin \
            -e ACTIVEMQ_PASSWORD=admin \
            apache/activemq-classic:latest
          sudo docker run -itd --network ds-network -p 8080:80 \
            --name ds-ee-dev \
            -e AMQP_TYPE=activemq \
            -e AMQP_URI=amqp://admin:admin@onlyoffice-mq \
            -e JWT_ENABLED=true \
            -e JWT_SECRET=MYSECRET \
            -e WOPI_ENABLED=true \
            onlyoffice/4testing-documentserver-ee:${{ vars.ONLYOFFICE_DEV_VERSION }}.${{ vars.ONLYOFFICE_DEV_BUILD }}
          EOF

      - name: Wait for DS startup 240
        run: sleep 240

      - name: Add example host to DS ipfilter
        continue-on-error: true
        uses: ./.github/actions/set-ipfilter
        with:
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          allow_hosts: ${{ vars.AWS_DEV_SERVER_DOMAIN }} ${{ vars.AWS_DEV_SERVER_EC2_IP }}

      - name: AMQP_CLASSIC Enable nginx config
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo rm -f /etc/nginx/sites-enabled/* && \
             sudo ln -s /etc/nginx/sites-available/default_proxy-https-to-http.conf /etc/nginx/sites-enabled/default_proxy-https-to-http.conf && \
             sudo nginx -t && sudo systemctl reload nginx"

      - name: AMQP_CLASSIC Info
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo apt-get install jq -y
          echo "-------------healthcheck-------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck; echo
          echo "-------------DS services-------------"
          sudo docker exec ds-ee-dev supervisorctl status all || true
          echo "---------Installed packages----------"
          sudo docker exec ds-ee-dev bash -c "dpkg -l | grep onlyoffice"
          echo "--------packageType--------"
          sudo docker exec ds-ee-dev curl -s http://127.0.0.1/info/info.json | jq | grep -i -e buildVersion -e buildNumber -e packageType
          echo "-------------WOPI--------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/hosting/discovery | tail -c 150; echo
          EOF

      - name: AMQP_CLASSIC Check healthcheck
        continue-on-error: true
        run: |
          HC=$(curl -s "https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck" || echo "error")
          echo "Healthcheck: $HC"
          echo "HEALTHCHECK_AMQP_CLASSIC=$HC" >> $GITHUB_ENV

      - name: AMQP_CLASSIC Check version
        continue-on-error: true
        run: |
          VERSION=$(ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev dpkg-query -W onlyoffice-documentserver-ee" | awk '{print $2}')
          EXPECTED="${{ vars.ONLYOFFICE_DEV_VERSION }}-${{ vars.ONLYOFFICE_DEV_BUILD }}"
          echo "Version: installed=$VERSION, expected=$EXPECTED"
          if [ "$VERSION" = "$EXPECTED" ]; then
            echo "VERSION_AMQP_CLASSIC_OK=true" >> $GITHUB_ENV
          else
            echo "VERSION_AMQP_CLASSIC_OK=false" >> $GITHUB_ENV
          fi
          echo "VERSION_AMQP_CLASSIC_ACTUAL=$VERSION" >> $GITHUB_ENV

      - name: AMQP_CLASSIC Start ds-example
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev supervisorctl start ds:example"

      - name: AMQP_CLASSIC Clear puppeteer output
        run: rm -rf Dep.Tests/puppeteer/out

      - name: AMQP_CLASSIC Puppeteer API tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example

      - name: AMQP_CLASSIC Puppeteer WOPI tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          # vsdx.js WOPI test disabled — feature not finished yet (fails on WOPI provider)
          # python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi

      - name: AMQP_CLASSIC Parse puppeteer results
        if: always()
        run: python3 .github/scripts/parse-puppeteer-results.py --edition AMQP_CLASSIC

      - name: AMQP_CLASSIC Documentserver logs
        continue-on-error: true
        uses: ./.github/actions/ds-logs
        with:
          mode: ssh-docker
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev

      - name: AMQP_CLASSIC Check DS logs for errors
        continue-on-error: true
        uses: ./.github/actions/check-ds-logs
        with:
          mode: ssh-docker-logs
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          env_var: DS_LOG_ERRORS_AMQP_CLASSIC
          label: AMQP_CLASSIC

      - name: REDIS_REDIS Docker reInstall
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker stop \$(sudo docker ps -q) 2>/dev/null || true
          sudo docker rm \$(sudo docker ps -aq) 2>/dev/null || true
          sudo docker network rm ds-network 2>/dev/null || true
          sudo docker run -itd -p 8080:80 \
            --name ds-ee-dev \
            -e WOPI_ENABLED=true \
            onlyoffice/4testing-documentserver-ee:${{ vars.ONLYOFFICE_DEV_VERSION }}.${{ vars.ONLYOFFICE_DEV_BUILD }}
          EOF

      - name: Wait for DS startup 240
        run: sleep 240

      - name: Add example host to DS ipfilter
        continue-on-error: true
        uses: ./.github/actions/set-ipfilter
        with:
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          allow_hosts: ${{ vars.AWS_DEV_SERVER_DOMAIN }} ${{ vars.AWS_DEV_SERVER_EC2_IP }}

      - name: REDIS_REDIS Enable nginx config
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo rm -f /etc/nginx/sites-enabled/* && \
             sudo ln -s /etc/nginx/sites-available/default_proxy-https-to-http.conf /etc/nginx/sites-enabled/default_proxy-https-to-http.conf && \
             sudo nginx -t && sudo systemctl reload nginx"

      - name: REDIS_REDIS Configure unix.sock
        continue-on-error: true
        run: |
          # Install now generates a random redis password in local.json; pull it and inject
          # it into our socket config (redis driver reads options.password) and into ping.
          REDIS_PASS=$(ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev cat /etc/onlyoffice/documentserver/local.json | jq -r '.services.CoAuthoring.redis.options.password // empty'")
          echo "REDIS_PASS_REDIS_REDIS=$REDIS_PASS" >> $GITHUB_ENV
          echo "Redis password from local.json: '$REDIS_PASS'"
          AUTH=""; [ -n "$REDIS_PASS" ] && AUTH="-a $REDIS_PASS"
          if [ -n "$REDIS_PASS" ]; then
            jq --arg p "$REDIS_PASS" '.services.CoAuthoring.redis.options.password = $p' \
              redis/redis-unix-sock/local-production-linux.json > /tmp/lp.json
          else
            cp redis/redis-unix-sock/local-production-linux.json /tmp/lp.json
          fi
          scp -o StrictHostKeyChecking=no /tmp/lp.json \
            ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }}:~/local-production-linux.json
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          echo "------------ STOP DS services -------------"
          sudo docker exec ds-ee-dev supervisorctl stop ds:converter ds:docservice ds:adminpanel
          echo "------------ Setup unix.sock and STOP redis server -------------"
          sudo docker exec ds-ee-dev bash -c "sed -i 's/^port .*/port 0/' /etc/redis/redis.conf && echo 'unixsocket /run/redis/redis-server.sock' >> /etc/redis/redis.conf && echo 'unixsocketperm 777' >> /etc/redis/redis.conf && pkill -f redis-server"
          sleep 10
          echo "------------ START REDIS server -------------"
          sudo docker exec ds-ee-dev service redis-server start
          echo "------------ Redis ping -------------"
          sudo docker exec ds-ee-dev redis-cli -s /run/redis/redis-server.sock $AUTH ping
          echo "------------ Redis port 6379 must be closed -------------"
          sudo docker exec ds-ee-dev netstat -tulpn
          echo "------------ Copy DS local-production-linux.json config -------------"
          sudo docker cp ~/local-production-linux.json ds-ee-dev:/etc/onlyoffice/documentserver/local-production-linux.json
          sudo rm ~/local-production-linux.json
          sudo docker exec ds-ee-dev cat /etc/onlyoffice/documentserver/local-production-linux.json
          echo "------------ START DS services -------------"
          sudo docker exec ds-ee-dev supervisorctl start ds:converter ds:docservice ds:adminpanel
          sleep 10
          EOF

      - name: REDIS_REDIS Info
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          echo "-------------healthcheck-------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck; echo
          echo "-------------DS services-------------"
          sudo docker exec ds-ee-dev supervisorctl status all || true
          echo "---------Installed packages----------"
          sudo docker exec ds-ee-dev bash -c "dpkg -l | grep onlyoffice"
          echo "--------packageType--------"
          sudo docker exec ds-ee-dev curl -s http://127.0.0.1/info/info.json | jq | grep -i -e buildVersion -e buildNumber -e packageType
          echo "-------------WOPI--------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/hosting/discovery | tail -c 150; echo
          EOF

      - name: REDIS_REDIS Check healthcheck and socket
        continue-on-error: true
        run: |
          HC=$(curl -s "https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck" || echo "error")
          echo "Healthcheck: $HC"
          echo "HEALTHCHECK_REDIS_REDIS=$HC" >> $GITHUB_ENV

          AUTH=""; [ -n "$REDIS_PASS_REDIS_REDIS" ] && AUTH="-a $REDIS_PASS_REDIS_REDIS"
          PING=$(ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev redis-cli -s /run/redis/redis-server.sock $AUTH ping 2>/dev/null || echo 'error'")
          echo "Redis socket ping: $PING"
          if [ "$PING" = "PONG" ]; then
            echo "REDIS_SOCK_OK_REDIS_REDIS=true" >> $GITHUB_ENV
          else
            echo "REDIS_SOCK_OK_REDIS_REDIS=false" >> $GITHUB_ENV
          fi

          PORT_CHECK=$(ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev netstat -tulpn 2>/dev/null | grep ':6379' || echo ''")
          echo "Port 6379 check: '${PORT_CHECK}'"
          if [ -z "$PORT_CHECK" ]; then
            echo "PORT_6379_CLOSED_REDIS_REDIS=true" >> $GITHUB_ENV
          else
            echo "PORT_6379_CLOSED_REDIS_REDIS=false" >> $GITHUB_ENV
          fi

      - name: REDIS_REDIS Check version
        continue-on-error: true
        run: |
          VERSION=$(ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev dpkg-query -W onlyoffice-documentserver-ee" | awk '{print $2}')
          EXPECTED="${{ vars.ONLYOFFICE_DEV_VERSION }}-${{ vars.ONLYOFFICE_DEV_BUILD }}"
          echo "Version: installed=$VERSION, expected=$EXPECTED"
          if [ "$VERSION" = "$EXPECTED" ]; then
            echo "VERSION_REDIS_REDIS_OK=true" >> $GITHUB_ENV
          else
            echo "VERSION_REDIS_REDIS_OK=false" >> $GITHUB_ENV
          fi
          echo "VERSION_REDIS_REDIS_ACTUAL=$VERSION" >> $GITHUB_ENV

      - name: REDIS_REDIS Start ds-example
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev supervisorctl start ds:example"

      - name: REDIS_REDIS Clear puppeteer output
        run: rm -rf Dep.Tests/puppeteer/out

      - name: REDIS_REDIS Puppeteer API tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          sed -i -E "s#\"url\": \"[^\"]+/example\"#\"url\": \"https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/example\"#" ../config_chrome_linux.json
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example

      - name: REDIS_REDIS Puppeteer WOPI tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          # vsdx.js WOPI test disabled — feature not finished yet (fails on WOPI provider)
          # python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi

      - name: REDIS_REDIS Parse puppeteer results
        if: always()
        run: python3 .github/scripts/parse-puppeteer-results.py --edition REDIS_REDIS

      - name: REDIS_REDIS Documentserver logs
        continue-on-error: true
        uses: ./.github/actions/ds-logs
        with:
          mode: ssh-docker
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev

      - name: REDIS_REDIS Check DS logs for errors
        continue-on-error: true
        uses: ./.github/actions/check-ds-logs
        with:
          mode: ssh-docker-logs
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          env_var: DS_LOG_ERRORS_REDIS_REDIS
          label: REDIS_REDIS

      - name: REDIS_IOREDIS Docker reInstall
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          sudo docker stop \$(sudo docker ps -q) 2>/dev/null || true
          sudo docker rm \$(sudo docker ps -aq) 2>/dev/null || true
          sudo docker network rm ds-network 2>/dev/null || true
          sudo docker run -itd -p 8080:80 \
            --name ds-ee-dev \
            -e WOPI_ENABLED=true \
            onlyoffice/4testing-documentserver-ee:${{ vars.ONLYOFFICE_DEV_VERSION }}.${{ vars.ONLYOFFICE_DEV_BUILD }}
          EOF

      - name: Wait for DS startup 240
        run: sleep 240

      - name: Add example host to DS ipfilter
        continue-on-error: true
        uses: ./.github/actions/set-ipfilter
        with:
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          allow_hosts: ${{ vars.AWS_DEV_SERVER_DOMAIN }} ${{ vars.AWS_DEV_SERVER_EC2_IP }}

      - name: REDIS_IOREDIS Enable nginx config
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo rm -f /etc/nginx/sites-enabled/* && \
             sudo ln -s /etc/nginx/sites-available/default_proxy-https-to-http.conf /etc/nginx/sites-enabled/default_proxy-https-to-http.conf && \
             sudo nginx -t && sudo systemctl reload nginx"

      - name: REDIS_IOREDIS Configure unix.sock
        continue-on-error: true
        run: |
          # Install now generates a random redis password in local.json; pull it and inject
          # it into our socket config (ioredis driver reads iooptions.password) and into ping.
          REDIS_PASS=$(ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev cat /etc/onlyoffice/documentserver/local.json | jq -r '.services.CoAuthoring.redis.options.password // empty'")
          echo "REDIS_PASS_REDIS_IOREDIS=$REDIS_PASS" >> $GITHUB_ENV
          echo "Redis password from local.json: '$REDIS_PASS'"
          AUTH=""; [ -n "$REDIS_PASS" ] && AUTH="-a $REDIS_PASS"
          if [ -n "$REDIS_PASS" ]; then
            jq --arg p "$REDIS_PASS" '.services.CoAuthoring.redis.iooptions.password = $p' \
              redis/ioredis-unix-sock/local-production-linux.json > /tmp/lp.json
          else
            cp redis/ioredis-unix-sock/local-production-linux.json /tmp/lp.json
          fi
          scp -o StrictHostKeyChecking=no /tmp/lp.json \
            ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }}:~/local-production-linux.json
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          echo "------------ STOP DS services -------------"
          sudo docker exec ds-ee-dev supervisorctl stop ds:converter ds:docservice ds:adminpanel
          echo "------------ Setup unix.sock and STOP redis server -------------"
          sudo docker exec ds-ee-dev bash -c "sed -i 's/^port .*/port 0/' /etc/redis/redis.conf && echo 'unixsocket /run/redis/redis-server.sock' >> /etc/redis/redis.conf && echo 'unixsocketperm 777' >> /etc/redis/redis.conf && pkill -f redis-server"
          sleep 10
          echo "------------ START REDIS server -------------"
          sudo docker exec ds-ee-dev service redis-server start
          echo "------------ Redis ping -------------"
          sudo docker exec ds-ee-dev redis-cli -s /run/redis/redis-server.sock $AUTH ping
          echo "------------ Redis port 6379 must be closed -------------"
          sudo docker exec ds-ee-dev netstat -tulpn
          echo "------------ Copy DS local-production-linux.json config -------------"
          sudo docker cp ~/local-production-linux.json ds-ee-dev:/etc/onlyoffice/documentserver/local-production-linux.json
          sudo rm ~/local-production-linux.json
          sudo docker exec ds-ee-dev cat /etc/onlyoffice/documentserver/local-production-linux.json
          echo "------------ START DS services -------------"
          sudo docker exec ds-ee-dev supervisorctl start ds:converter ds:docservice ds:adminpanel
          sleep 10
          EOF

      - name: REDIS_IOREDIS Info
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} << EOF
          echo "-------------healthcheck-------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck; echo
          echo "-------------DS services-------------"
          sudo docker exec ds-ee-dev supervisorctl status all || true
          echo "---------Installed packages----------"
          sudo docker exec ds-ee-dev bash -c "dpkg -l | grep onlyoffice"
          echo "--------packageType--------"
          sudo docker exec ds-ee-dev curl -s http://127.0.0.1/info/info.json | jq | grep -i -e buildVersion -e buildNumber -e packageType
          echo "-------------WOPI--------------"
          curl -s https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/hosting/discovery | tail -c 150; echo
          EOF

      - name: REDIS_IOREDIS Check healthcheck and socket
        continue-on-error: true
        run: |
          HC=$(curl -s "https://${{ vars.AWS_DEV_SERVER_DOMAIN }}/healthcheck" || echo "error")
          echo "Healthcheck: $HC"
          echo "HEALTHCHECK_REDIS_IOREDIS=$HC" >> $GITHUB_ENV

          AUTH=""; [ -n "$REDIS_PASS_REDIS_IOREDIS" ] && AUTH="-a $REDIS_PASS_REDIS_IOREDIS"
          PING=$(ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev redis-cli -s /run/redis/redis-server.sock $AUTH ping 2>/dev/null || echo 'error'")
          echo "Redis socket ping: $PING"
          if [ "$PING" = "PONG" ]; then
            echo "REDIS_SOCK_OK_REDIS_IOREDIS=true" >> $GITHUB_ENV
          else
            echo "REDIS_SOCK_OK_REDIS_IOREDIS=false" >> $GITHUB_ENV
          fi

          PORT_CHECK=$(ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev netstat -tulpn 2>/dev/null | grep ':6379' || echo ''")
          echo "Port 6379 check: '${PORT_CHECK}'"
          if [ -z "$PORT_CHECK" ]; then
            echo "PORT_6379_CLOSED_REDIS_IOREDIS=true" >> $GITHUB_ENV
          else
            echo "PORT_6379_CLOSED_REDIS_IOREDIS=false" >> $GITHUB_ENV
          fi

      - name: REDIS_IOREDIS Check version
        continue-on-error: true
        run: |
          VERSION=$(ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev dpkg-query -W onlyoffice-documentserver-ee" | awk '{print $2}')
          EXPECTED="${{ vars.ONLYOFFICE_DEV_VERSION }}-${{ vars.ONLYOFFICE_DEV_BUILD }}"
          echo "Version: installed=$VERSION, expected=$EXPECTED"
          if [ "$VERSION" = "$EXPECTED" ]; then
            echo "VERSION_REDIS_IOREDIS_OK=true" >> $GITHUB_ENV
          else
            echo "VERSION_REDIS_IOREDIS_OK=false" >> $GITHUB_ENV
          fi
          echo "VERSION_REDIS_IOREDIS_ACTUAL=$VERSION" >> $GITHUB_ENV

      - name: REDIS_IOREDIS Start ds-example
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker exec ds-ee-dev supervisorctl start ds:example"

      - name: REDIS_IOREDIS Clear puppeteer output
        run: rm -rf Dep.Tests/puppeteer/out

      - name: REDIS_IOREDIS Puppeteer API tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example
          python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers example

      - name: REDIS_IOREDIS Puppeteer WOPI tests
        continue-on-error: true
        run: |
          cd Dep.Tests/puppeteer/engine
          echo "-------------------------------------"
          jq -r '.testOptions.url' ../config_chrome_linux.json
          echo "-------------------------------------"
          python3 run.py ../tests/web/desktop/word/input/input_text.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/spreadsheets/hometab/font/font_bold.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/presentation/hometab/input/input_text_and_download.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf/smoke/check_find_for_a_specific_pdf.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          python3 run.py ../tests/web/desktop/pdf-form/rightmenu/insert_checkbox.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi
          # vsdx.js WOPI test disabled — feature not finished yet (fails on WOPI provider)
          # python3 run.py ../tests/web/desktop/openers/vsdx.js --disable_animation --config_params puppeteerOptions.headless=true puppeteerOptions.channel=chrome --providers wopi

      - name: REDIS_IOREDIS Parse puppeteer results
        if: always()
        run: python3 .github/scripts/parse-puppeteer-results.py --edition REDIS_IOREDIS

      - name: REDIS_IOREDIS Documentserver logs
        continue-on-error: true
        uses: ./.github/actions/ds-logs
        with:
          mode: ssh-docker
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev

      - name: REDIS_IOREDIS Check DS logs for errors
        continue-on-error: true
        uses: ./.github/actions/check-ds-logs
        with:
          mode: ssh-docker-logs
          ssh_host: ${{ vars.AWS_DEV_SERVER_EC2_IP }}
          container: ds-ee-dev
          env_var: DS_LOG_ERRORS_REDIS_IOREDIS
          label: REDIS_IOREDIS

      - name: Final check
        if: always()
        env:
          EXPECTED_VERSION: "${{ vars.ONLYOFFICE_DEV_VERSION }}-${{ vars.ONLYOFFICE_DEV_BUILD }}"
        run: bash .github/scripts/dev-server-checks/final-check.sh

      - name: Save results
        if: always()
        continue-on-error: true
        run: |
          mkdir -p .github/workflow-results
          python3 - << 'PYEOF'
          import json, os
          from datetime import datetime, timezone

          def b(v): return os.environ.get(v, 'false') == 'true'
          def n(v): return int(os.environ.get(v, '0') or '0')
          def s(v): return os.environ.get(v, 'not set')

          # puppeteer passes only when nothing failed AND something actually ran
          def ppt_ok(failed, total): return failed == 0 and total > 0

          s3f_ppt      = n('PUPPETEER_AWS_S3_FALSE_FAILED')
          s3f_ok       = n('PUPPETEER_AWS_S3_FALSE_OK')
          s3f_total    = n('PUPPETEER_AWS_S3_FALSE_TOTAL')
          s3t_ppt      = n('PUPPETEER_AWS_S3_TRUE_FAILED')
          s3t_ok       = n('PUPPETEER_AWS_S3_TRUE_OK')
          s3t_total    = n('PUPPETEER_AWS_S3_TRUE_TOTAL')
          sp_ppt       = n('PUPPETEER_AWS_S3_PATH_STYLE_FAILED')
          sp_ok        = n('PUPPETEER_AWS_S3_PATH_STYLE_OK')
          sp_total     = n('PUPPETEER_AWS_S3_PATH_STYLE_TOTAL')
          kms_ppt      = n('PUPPETEER_AWS_S3_KMS_FAILED')
          kms_ok       = n('PUPPETEER_AWS_S3_KMS_OK')
          kms_total    = n('PUPPETEER_AWS_S3_KMS_TOTAL')
          azf_ppt      = n('PUPPETEER_AZURE_STORAGE_DIRECTURL_FALSE_FAILED')
          azf_ok       = n('PUPPETEER_AZURE_STORAGE_DIRECTURL_FALSE_OK')
          azf_total    = n('PUPPETEER_AZURE_STORAGE_DIRECTURL_FALSE_TOTAL')
          azt_ppt      = n('PUPPETEER_AZURE_STORAGE_DIRECTURL_TRUE_FAILED')
          azt_ok       = n('PUPPETEER_AZURE_STORAGE_DIRECTURL_TRUE_OK')
          azt_total    = n('PUPPETEER_AZURE_STORAGE_DIRECTURL_TRUE_TOTAL')
          aze_ppt      = n('PUPPETEER_AZURE_STORAGE_ENCRYPTION_SCOPE_FAILED')
          aze_ok       = n('PUPPETEER_AZURE_STORAGE_ENCRYPTION_SCOPE_OK')
          aze_total    = n('PUPPETEER_AZURE_STORAGE_ENCRYPTION_SCOPE_TOTAL')
          vp_ppt       = n('PUPPETEER_DS_VPATH_FAILED')
          vp_ok        = n('PUPPETEER_DS_VPATH_OK')
          vp_total     = n('PUPPETEER_DS_VPATH_TOTAL')
          art_ppt      = n('PUPPETEER_AMQP_ARTEMIS_FAILED')
          art_ok       = n('PUPPETEER_AMQP_ARTEMIS_OK')
          art_total    = n('PUPPETEER_AMQP_ARTEMIS_TOTAL')
          cls_ppt      = n('PUPPETEER_AMQP_CLASSIC_FAILED')
          cls_ok       = n('PUPPETEER_AMQP_CLASSIC_OK')
          cls_total    = n('PUPPETEER_AMQP_CLASSIC_TOTAL')
          rr_ppt       = n('PUPPETEER_REDIS_REDIS_FAILED')
          rr_ok        = n('PUPPETEER_REDIS_REDIS_OK')
          rr_total     = n('PUPPETEER_REDIS_REDIS_TOTAL')
          ri_ppt       = n('PUPPETEER_REDIS_IOREDIS_FAILED')
          ri_ok        = n('PUPPETEER_REDIS_IOREDIS_OK')
          ri_total     = n('PUPPETEER_REDIS_IOREDIS_TOTAL')
          s3f_ds  = n('DS_LOG_ERRORS_AWS_S3_FALSE')
          s3t_ds  = n('DS_LOG_ERRORS_AWS_S3_TRUE')
          sp_ds   = n('DS_LOG_ERRORS_AWS_S3_PATH_STYLE')
          kms_ds  = n('DS_LOG_ERRORS_AWS_S3_KMS')
          azf_ds  = n('DS_LOG_ERRORS_AZURE_STORAGE_DIRECTURL_FALSE')
          azt_ds  = n('DS_LOG_ERRORS_AZURE_STORAGE_DIRECTURL_TRUE')
          aze_ds  = n('DS_LOG_ERRORS_AZURE_STORAGE_ENCRYPTION_SCOPE')
          vp_ds   = n('DS_LOG_ERRORS_DS_VPATH')
          art_ds  = n('DS_LOG_ERRORS_AMQP_ARTEMIS')
          cls_ds  = n('DS_LOG_ERRORS_AMQP_CLASSIC')
          rr_ds   = n('DS_LOG_ERRORS_REDIS_REDIS')
          ri_ds   = n('DS_LOG_ERRORS_REDIS_IOREDIS')

          data = {
              'run_date': datetime.now(timezone.utc).strftime('%Y-%m-%d %H:%M UTC'),
              's3_false': {
                  'healthy':               b('HEALTHCHECK_AWS_S3_FALSE'),
                  'version_ok':            b('VERSION_AWS_S3_FALSE_OK'),
                  'version_actual':        s('VERSION_AWS_S3_FALSE_ACTUAL'),
                  'puppeteer_failed':      s3f_ppt,
                  'puppeteer_ok':          s3f_ok,
                  'puppeteer_total':       s3f_total,
                  'ds_log_errors':         s3f_ds,
                  'conclusion': 'success' if (b('HEALTHCHECK_AWS_S3_FALSE') and b('VERSION_AWS_S3_FALSE_OK') and ppt_ok(s3f_ppt, s3f_total) and s3f_ds == 0) else 'failure',
              },
              's3_true': {
                  'healthy':               b('HEALTHCHECK_AWS_S3_TRUE'),
                  'version_ok':            b('VERSION_AWS_S3_TRUE_OK'),
                  'version_actual':        s('VERSION_AWS_S3_TRUE_ACTUAL'),
                  'puppeteer_failed':      s3t_ppt,
                  'puppeteer_ok':          s3t_ok,
                  'puppeteer_total':       s3t_total,
                  'ds_log_errors':         s3t_ds,
                  'conclusion': 'success' if (b('HEALTHCHECK_AWS_S3_TRUE') and b('VERSION_AWS_S3_TRUE_OK') and ppt_ok(s3t_ppt, s3t_total) and s3t_ds == 0) else 'failure',
              },
              's3_path_style': {
                  'healthy':               b('HEALTHCHECK_AWS_S3_PATH_STYLE'),
                  'version_ok':            b('VERSION_AWS_S3_PATH_STYLE_OK'),
                  'version_actual':        s('VERSION_AWS_S3_PATH_STYLE_ACTUAL'),
                  'puppeteer_failed':      sp_ppt,
                  'puppeteer_ok':          sp_ok,
                  'puppeteer_total':       sp_total,
                  'ds_log_errors':         sp_ds,
                  'conclusion': 'success' if (b('HEALTHCHECK_AWS_S3_PATH_STYLE') and b('VERSION_AWS_S3_PATH_STYLE_OK') and ppt_ok(sp_ppt, sp_total) and sp_ds == 0) else 'failure',
              },
              's3_kms': {
                  'healthy':               b('HEALTHCHECK_AWS_S3_KMS'),
                  'version_ok':            b('VERSION_AWS_S3_KMS_OK'),
                  'version_actual':        s('VERSION_AWS_S3_KMS_ACTUAL'),
                  'puppeteer_failed':      kms_ppt,
                  'puppeteer_ok':          kms_ok,
                  'puppeteer_total':       kms_total,
                  'ds_log_errors':         kms_ds,
                  'conclusion': 'success' if (b('HEALTHCHECK_AWS_S3_KMS') and b('VERSION_AWS_S3_KMS_OK') and ppt_ok(kms_ppt, kms_total) and kms_ds == 0) else 'failure',
              },
              'az_false': {
                  'healthy':               b('HEALTHCHECK_AZURE_STORAGE_DIRECTURL_FALSE'),
                  'version_ok':            b('VERSION_AZURE_STORAGE_DIRECTURL_FALSE_OK'),
                  'version_actual':        s('VERSION_AZURE_STORAGE_DIRECTURL_FALSE_ACTUAL'),
                  'puppeteer_failed':      azf_ppt,
                  'puppeteer_ok':          azf_ok,
                  'puppeteer_total':       azf_total,
                  'ds_log_errors':         azf_ds,
                  'conclusion': 'success' if (b('HEALTHCHECK_AZURE_STORAGE_DIRECTURL_FALSE') and b('VERSION_AZURE_STORAGE_DIRECTURL_FALSE_OK') and ppt_ok(azf_ppt, azf_total) and azf_ds == 0) else 'failure',
              },
              'az_true': {
                  'healthy':               b('HEALTHCHECK_AZURE_STORAGE_DIRECTURL_TRUE'),
                  'version_ok':            b('VERSION_AZURE_STORAGE_DIRECTURL_TRUE_OK'),
                  'version_actual':        s('VERSION_AZURE_STORAGE_DIRECTURL_TRUE_ACTUAL'),
                  'puppeteer_failed':      azt_ppt,
                  'puppeteer_ok':          azt_ok,
                  'puppeteer_total':       azt_total,
                  'ds_log_errors':         azt_ds,
                  'conclusion': 'success' if (b('HEALTHCHECK_AZURE_STORAGE_DIRECTURL_TRUE') and b('VERSION_AZURE_STORAGE_DIRECTURL_TRUE_OK') and ppt_ok(azt_ppt, azt_total) and azt_ds == 0) else 'failure',
              },
              'az_encryption_scope': {
                  'healthy':               b('HEALTHCHECK_AZURE_STORAGE_ENCRYPTION_SCOPE'),
                  'version_ok':            b('VERSION_AZURE_STORAGE_ENCRYPTION_SCOPE_OK'),
                  'version_actual':        s('VERSION_AZURE_STORAGE_ENCRYPTION_SCOPE_ACTUAL'),
                  'puppeteer_failed':      aze_ppt,
                  'puppeteer_ok':          aze_ok,
                  'puppeteer_total':       aze_total,
                  'ds_log_errors':         aze_ds,
                  'conclusion': 'success' if (b('HEALTHCHECK_AZURE_STORAGE_ENCRYPTION_SCOPE') and b('VERSION_AZURE_STORAGE_ENCRYPTION_SCOPE_OK') and ppt_ok(aze_ppt, aze_total) and aze_ds == 0) else 'failure',
              },
              'vpath': {
                  'healthy':               b('HEALTHCHECK_DS_VPATH'),
                  'version_ok':            b('VERSION_DS_VPATH_OK'),
                  'version_actual':        s('VERSION_DS_VPATH_ACTUAL'),
                  'puppeteer_failed':      vp_ppt,
                  'puppeteer_ok':          vp_ok,
                  'puppeteer_total':       vp_total,
                  'ds_log_errors':         vp_ds,
                  'conclusion': 'success' if (b('HEALTHCHECK_DS_VPATH') and b('VERSION_DS_VPATH_OK') and ppt_ok(vp_ppt, vp_total) and vp_ds == 0) else 'failure',
              },
              'amqp_artemis': {
                  'healthy':               b('HEALTHCHECK_AMQP_ARTEMIS'),
                  'version_ok':            b('VERSION_AMQP_ARTEMIS_OK'),
                  'version_actual':        s('VERSION_AMQP_ARTEMIS_ACTUAL'),
                  'puppeteer_failed':      art_ppt,
                  'puppeteer_ok':          art_ok,
                  'puppeteer_total':       art_total,
                  'ds_log_errors':         art_ds,
                  'conclusion': 'success' if (b('HEALTHCHECK_AMQP_ARTEMIS') and b('VERSION_AMQP_ARTEMIS_OK') and ppt_ok(art_ppt, art_total) and art_ds == 0) else 'failure',
              },
              'amqp_classic': {
                  'healthy':               b('HEALTHCHECK_AMQP_CLASSIC'),
                  'version_ok':            b('VERSION_AMQP_CLASSIC_OK'),
                  'version_actual':        s('VERSION_AMQP_CLASSIC_ACTUAL'),
                  'puppeteer_failed':      cls_ppt,
                  'puppeteer_ok':          cls_ok,
                  'puppeteer_total':       cls_total,
                  'ds_log_errors':         cls_ds,
                  'conclusion': 'success' if (b('HEALTHCHECK_AMQP_CLASSIC') and b('VERSION_AMQP_CLASSIC_OK') and ppt_ok(cls_ppt, cls_total) and cls_ds == 0) else 'failure',
              },
              'redis_redis': {
                  'healthy':               b('HEALTHCHECK_REDIS_REDIS'),
                  'version_ok':            b('VERSION_REDIS_REDIS_OK'),
                  'version_actual':        s('VERSION_REDIS_REDIS_ACTUAL'),
                  'redis_sock_ok':         b('REDIS_SOCK_OK_REDIS_REDIS'),
                  'port_6379_closed':      b('PORT_6379_CLOSED_REDIS_REDIS'),
                  'puppeteer_failed':      rr_ppt,
                  'puppeteer_ok':          rr_ok,
                  'puppeteer_total':       rr_total,
                  'ds_log_errors':         rr_ds,
                  'conclusion': 'success' if (b('HEALTHCHECK_REDIS_REDIS') and b('VERSION_REDIS_REDIS_OK') and b('REDIS_SOCK_OK_REDIS_REDIS') and b('PORT_6379_CLOSED_REDIS_REDIS') and ppt_ok(rr_ppt, rr_total) and rr_ds == 0) else 'failure',
              },
              'redis_ioredis': {
                  'healthy':               b('HEALTHCHECK_REDIS_IOREDIS'),
                  'version_ok':            b('VERSION_REDIS_IOREDIS_OK'),
                  'version_actual':        s('VERSION_REDIS_IOREDIS_ACTUAL'),
                  'redis_sock_ok':         b('REDIS_SOCK_OK_REDIS_IOREDIS'),
                  'port_6379_closed':      b('PORT_6379_CLOSED_REDIS_IOREDIS'),
                  'puppeteer_failed':      ri_ppt,
                  'puppeteer_ok':          ri_ok,
                  'puppeteer_total':       ri_total,
                  'ds_log_errors':         ri_ds,
                  'conclusion': 'success' if (b('HEALTHCHECK_REDIS_IOREDIS') and b('VERSION_REDIS_IOREDIS_OK') and b('REDIS_SOCK_OK_REDIS_IOREDIS') and b('PORT_6379_CLOSED_REDIS_IOREDIS') and ppt_ok(ri_ppt, ri_total) and ri_ds == 0) else 'failure',
              },
              'conclusion': os.environ.get('FINAL_CONCLUSION', 'failure'),
          }
          path = '.github/workflow-results/dev-server-checks.json'
          with open(path, 'w') as f:
              json.dump(data, f, indent=2)
          print(f'Saved: {path}')
          PYEOF

          aws s3 cp .github/workflow-results/dev-server-checks.json \
            s3://${{ vars.DASHBOARD_BUCKET_NAME }}/results/dev-server-checks.json \
            --region ${{ vars.DASHBOARD_BUCKET_REGION }}

      - name: Clear S3 bucket files folder
        if: always()
        run: |
          sleep 10 && aws s3 rm s3://${{ secrets.AWS_DEV_SERVER_BUCKET_NAME }}/files/ --recursive

      - name: Docker cleanup after run
        if: always()
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ vars.AWS_DEV_SERVER_EC2_IP }} \
            "sudo docker stop \$(sudo docker ps -q) 2>/dev/null || true && sudo docker system prune -af"

      - name: Stop EC2 Instance
        if: always()
        run: |
          aws ec2 stop-instances --instance-ids ${{ secrets.AWS_DEV_SERVER_INSTANCE_ID }}
          aws ec2 wait instance-stopped --instance-ids ${{ secrets.AWS_DEV_SERVER_INSTANCE_ID }}
